name: Gatsby Publish

on:
  push:
    branches:
      - main
env:
  GH_TOKEN: ${{ secrets.SECRETKEY}}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: enriikke/gatsby-gh-pages-action@v2
        with:
          access-token: ${{ secrets.SECRETKEY}}
          deploy-branch: gh-pages
          gatsby-args: --prefix-paths
